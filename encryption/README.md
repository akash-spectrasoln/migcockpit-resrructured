# DEPRECATED
This folder is superseded by `core/connections/encryption.py`.
The file `encryption.py` here is kept only for backward compatibility.
Update all imports to: `from core.connections.encryption import ...`
